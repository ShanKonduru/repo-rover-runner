-r requirements.txt
coverage>=7.6.0
pip-audit>=2.7.3
bandit>=1.7.9
sec-report-kit
mcp>=1.0.0

# External CLI dependency (not installable via pip):
# Install gitleaks separately and ensure it is available on PATH.
